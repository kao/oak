;; Global variables
(defvar oak-leader "SPC"
  "Oak key leader")

(defvar oak-major-mode-leader ","
  "Major mode leader")

(defvar oak-base-map (make-sparse-keymap)
  "Base keymap for all oak leader key commands.")

(provide 'oak-variable)
